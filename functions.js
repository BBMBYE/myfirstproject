/*const coffeeOrder = (type, size) => {
    console.log(` ${type}, and ${size} drink please`);
}
coffeeOrder("Latte", "Large");
coffeeOrder("flatwhite", "medium");
coffeeOrder("cappuccino", "small");*/

/*et orderCount = 0;
const takeOrder = (topping1,topping2, topping3) => {
    console.log(`Pizza with ${topping1}, ${topping2} and ${topping3}`)
    orderCount++;
    console.log(orderCount);
}
 takeOrder("beef","chicken","sweetcorn");
 takeOrder("beef","onions","pepper")*/
//let balance = 500;
//let pin = 1234;

/*const withdraw = (amount, account, pin) => {

if (balance >= amount && pin == 1234)
console.log(`Withdraw ${amount} from acount ${account}`);

else if(balance >= amount && pin != 1234){
console.log("wrong pin");}

else if(balance <= amount && pin == 1234){
    console.log("Insufficient funds");
}
else if(balance <= amount && pin != 1234){
    console.log("Insufficient funds and wrong pin")}}

withdraw(501, 67890, 1234);*/

const subOrder = (topping1, topping2, topping3, topping4, topping5) =>{
    console.log(`Subway with ${topping1}, ${topping2}, ${topping3}, ${topping4} and ${topping5} please.`)
};
subOrder("chicken", "onions", "cheese", "pepper", "mayo");

let filmCheck = "Godfather";

}
i
}
while(filmCheck != "Godfather"){
    console.log("Boo,we want the Godfather");
    filmcheck = films[Math.floor(Math.random)* 6];
}
console.log("Yay Godfather");